{
    "version": 2,
    "buildCommand": "cd frontend && npm install && NODE_OPTIONS=--openssl-legacy-provider npx react-scripts build",
    "outputDirectory": "frontend/build",
    "framework": null,
    "env": {
        "REACT_APP_USE_POLLING": "true",
        "REACT_APP_BACKEND_URL": ""
    },
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/$1"
        },
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}